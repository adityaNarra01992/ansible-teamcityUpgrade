#!/bin/bash

backup_dir="{{ backup_dir }}"
teamcity_installation_dir="{{ teamcity_installation_dir }}"
timestamp=$(date +%Y%m%d%H%M%S)

mkdir -p "$backup_dir/$timestamp"
cp -r "$teamcity_installation_dir/conf" "$backup_dir/$timestamp/"
# Add more backup steps if needed
